Perfect — let’s make a **complete file description guide** for your AutoShip project. This is extremely useful for judges, teammates, or future maintenance. I’ll describe **every folder and file in your tree** with its purpose and role in the system.

---

# **Project File Description: AutoShip**

---

## **.github/**

* `appmod/` → Placeholder for GitHub-specific workflows or app module testing. Currently empty (`appcat/` inside).

---

## **apps/dashboard/**

* Frontend UI for AutoShip:

  * Start runs
  * View logs and timeline
  * Show artifacts
  * Built with Vite + React + TypeScript

---

## **artifacts/**

* Auto-generated folder storing **immutable artifacts per run**:

  * `diff.patch` → git diff for what the agent changed
  * `test-results.txt` → test output
  * `build-output.txt` → build logs

---

## **backend/src/**

### **agents/**

* `agentTypes.ts` → Defines contracts for agents (Planner & Fixer). Includes `AgentContext`, `PlanStep`, and `AgentResult`.
* `plannerAgent.ts` → Creates initial workflow steps based on a prompt. Deterministic for hackathon.
* `fixerAgent.ts` → Reads failed step and applies a deterministic fix to workspace files.

---

### **artifacts/**

* `artifactWriter.ts` → Central helper to write artifacts to `/artifacts/<runId>/`.
* **collectors/**

  * `buildOutput.ts` → Extracts build-related logs and writes to `build-output.txt`.
  * `diff.ts` → Generates `diff.patch` from workspace git changes.
  * `testResults.ts` → Collects test output logs into `test-results.txt`.

---

### **config/**

* `env.ts` → Central environment configuration. Validates all env variables (PORT, NODE_ENV, WORKSPACE_ROOT, ARTIFACT_ROOT, RUN_TIMEOUT_MS) using Zod.

---

### **execution/**

* `commandRunner.ts` → Runs shell commands in workspace. Streams logs, enforces timeouts, returns exit codes.
* `sandbox.ts` → Guards allowed commands (`npm install`, `npm test`, `npm run build`) to prevent unsafe operations.
* `workspace.ts` → Creates a per-run isolated workspace, clones template project into `/workspaces/<runId>`.

---

### **index.ts**

* Backend entrypoint:

  * Starts Express server
  * Mounts `/api/runs` routes
  * Exposes `/health` endpoint

---

### **runs/**

* `runTypes.ts` → Defines `Run`, `RunStep`, `StepStatus`, and `RunStatus` types for the system.
* `runStore.ts` → In-memory store for runs. CRUD operations, updates steps and logs.
* `runManager.ts` → Optional helper to orchestrate run creation, start, and retrieval. (Can wrap `runStore` + workflow engine.)

---

### **server/**

* `index.ts` → Express server setup. Mounts routes, applies middleware like CORS and JSON parsing.
* `routes/runs.ts` → Exposes run API endpoints:

  * `POST /api/runs` → create a new run
  * `POST /api/runs/:id/start` → start the workflow
  * `GET /api/runs/:id` → fetch a single run
  * `GET /api/runs` → list all runs (summary)

---

### **telemetry/**

* `logger.ts` → Appends logs per run with timestamps.
* `timeline.ts` → Updates step status transitions (pending → running → completed/failed) and timestamps.

---

### **workflow/**

* `stepTypes.ts` → Defines `WorkflowStep` interface for workflow engine.
* `stepDefinitions.ts` → Default workflow steps: install dependencies, run tests, build.
* `workflowEngine.ts` → The main orchestration loop:

  * Creates workspace
  * Runs commands sequentially
  * Updates telemetry
  * Invokes fixer agent on failure
  * Collects artifacts at the end

---

## **backend/tests/**

* `health.test.ts` → Tests API endpoints and workflow engine with Vitest + Supertest:

  * Ensures run creation
  * Checks run status updates
  * Validates step completion

---

## **docs/**

* `architecture.md` → High-level design, folder structure, agent roles.
* `demo_script.md` → Step-by-step guide to run the demo.
* `threat_model.md` → Security considerations and sandboxing rationale.

---

## **README.md**

* Project overview and instructions for setup, running, and demo.

---

## **scripts/**

* `clean.sh` → Removes all `/workspaces` and `/artifacts` for a clean slate.
* `dev.sh` → Starts backend + dashboard locally for development.

---

## **SETUP.md**

* Installation instructions, environment setup, dependencies, and any OS-specific notes.

---

## **template/**

* Minimal project template cloned into `/workspaces/<runId>` for each run:

  * `package.json` → dependencies
  * `src/index.ts` → dummy code
  * `tests/sample.test.ts` → example tests
* Used to demo `npm install`, `npm test`, `npm run build`.

---

## **workspaces/**

* Dynamically generated folder during workflow execution.
* Each run gets its own folder: `/workspaces/<runId>/`
* Contains a clone of `template/` to run commands safely.
* **Never pre-populated.** Auto-generated by workflow engine.

---

✅ **Summary**

* Backend handles workflow orchestration, agents, telemetry, artifacts.
* Frontend/dashboard displays live updates.
* Template provides deterministic workspace for runs.
* Artifacts + logs + timeline make the demo convincing and auditable.

---


Ah! Now I understand clearly — your current setup is **simulation-first** or deterministic for hackathon, but you want to **switch it to real agents and real execution** so it runs autonomously from a prompt, not just fake/demo steps.

Let’s break this down carefully. I’ll list **exactly which files need changes**, what to change, and why.

---

# **Files that need changes for real agent-driven execution**

## 1️⃣ **Agents**

### Files:

* `backend/src/agents/plannerAgent.ts`
* `backend/src/agents/fixerAgent.ts`

### Why:

* Currently deterministic / hard-coded workflow steps.
* Needs to call a **real LLM (Gemini/OpenAI)** or any real decision-making logic.

### What to change:

* `plannerAgent.ts` → generate `PlanStep[]` from the user prompt using a real AI agent instead of returning hardcoded steps.
* `fixerAgent.ts` → optionally, read failure logs and generate fixes using AI (Gemini or OpenAI).

### Notes:

* Keep fallback deterministic steps if AI fails.
* These two are the **brains** of autonomous operation.

---

## 2️⃣ **Workflow Engine**

### File:

* `backend/src/workflow/workflowEngine.ts`

### Why:

* Currently the workflow engine assumes **fixed step IDs** from `stepDefinitions.ts`.
* For real agents, the workflow steps come dynamically from `plannerAgent`.

### What to change:

* Remove dependency on `stepDefinitions.ts` for initial steps.
* Use `plannerAgent(prompt)` to generate steps dynamically.
* Keep fixer agent logic as-is (trigger on failure).

### Optional:

* Inject agent logs to telemetry so you can display AI decisions in the dashboard.

---

## 3️⃣ **Step Definitions** (minor)

### File:

* `backend/src/workflow/stepDefinitions.ts`

### Why:

* This is currently hardcoded for simulation/demo.
* If switching to real agents, this becomes **optional** or only fallback for testing.

---

## 4️⃣ **Server / Routes**

### File:

* `backend/src/server/routes/runs.ts`

### Why:

* Currently `POST /api/runs` may call plannerAgent but feeds deterministic data.
* Needs to **accept a prompt from the frontend** and pass it to the plannerAgent to generate a real workflow.

### What to change:

* Allow `prompt` in request body.
* Planner agent returns real steps.
* Remove simulation toggles like `injectFailure` if using real AI + fixer.

---

## 5️⃣ **Optional / Helper Files**

* `backend/src/config/env.ts` → Add API keys for Gemini/OpenAI.
* `backend/src/execution/commandRunner.ts` → ensure supports dynamic commands from AI.
* `backend/src/telemetry/logger.ts` → optionally log agent reasoning for demo.

---

# **Files you probably do NOT need to change**

* `workspace.ts` → stays same, handles isolated workspaces.
* `sandbox.ts` → stays same, keeps commands safe.
* `artifactWriter.ts` + collectors → stays same, just writes AI-chosen commands output.
* `runStore.ts` / `runTypes.ts` → unchanged.
* `tests/` → you might extend to cover AI plans, but basic tests stay.
* `dashboard/` → may need small changes to allow prompt input instead of “start demo”.

---

# ✅ **Summary of key change points for “real agents” mode**

| Layer     | File                 | Change                                                     |
| --------- | -------------------- | ---------------------------------------------------------- |
| Agents    | `plannerAgent.ts`    | Use LLM / AI to generate real workflow steps               |
| Agents    | `fixerAgent.ts`      | Optional: AI-based fixes instead of deterministic patch    |
| Workflow  | `workflowEngine.ts`  | Accept dynamic steps from plannerAgent                     |
| Workflow  | `stepDefinitions.ts` | Optional fallback only                                     |
| Server    | `routes/runs.ts`     | Pass user prompt to plannerAgent, remove demo-only toggles |
| Config    | `env.ts`             | Add Gemini/OpenAI API keys                                 |
| Telemetry | `logger.ts`          | Optional: log AI reasoning for debugging                   |

---

⚡ **Next move suggestion:**
If you want, I can **rewrite `plannerAgent.ts` and `workflowEngine.ts` for real-agent mode**, ready to run LLM-driven autonomous workflow.

Do you want me to do that next?


{
  "compilerOptions": {
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true
  }
}
